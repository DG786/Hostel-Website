<?php //starting tag of php
$x= "I am a var.";
for ($i=7;$i<15;$i++){echo "I am the $i.th i<br>";}

echo "<b>Work of Application Program Interface is to ease the work of programmer. PHP has 

html tags.</b>";
// get to use json arrays
echo "<br>";
echo "<br>";

//FormS the input.
$form[1]= $_GET['radi1'];
$form[2]= $_GET['usern'];
$form[3]= $_GET['submit'];

echo json_encode($data); //json is an format which stores key & value pairs. i.e. Dictionary
?>